# Stable public Program Entry URLs

Public Program Entry details use `/programm/[id]`. Names may change or repeat, while the existing numeric ID keeps shared links stable without adding a public slug lifecycle.
