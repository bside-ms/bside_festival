# Every Schedule Entry is timed

AllDay / Ganztägig is dropped. Exhibitions and other “whole day” acts already have clock times, and the Slotplan has no AllDay rows left. One time model: every Schedule Entry has a start and an end. A long exhibition is a long timed block, not a separate placement kind. Leftover AllDay rows, if any on live, become timed before the mode goes away.
