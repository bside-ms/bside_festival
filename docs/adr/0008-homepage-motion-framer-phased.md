# Homepage motion uses Framer Motion and ships in phases

We add Framer Motion for public homepage animation instead of staying CSS-only. First public cut is motto build-up, continuous harbor waves, and the three-floor building; arrows, color splashes, and scroll reveals follow later so content and chrome are not blocked on a full illustration motion pass. Honor `prefers-reduced-motion` with static fallbacks.
