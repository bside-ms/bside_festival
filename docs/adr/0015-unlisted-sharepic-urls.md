# Unlisted Sharepic URLs without tokens

Sharepics live at `/programm/[id]/sharepic`. They are public and guessable from the Program Entry ID, but the public program does not link to them. Distribution is by email. Tokens were rejected because the image only restates already-public program data; a website button was rejected because the graphic is for acts to post, not a visitor feature.
