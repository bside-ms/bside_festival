# Sharepic URL is a page, the file is a download

`/programm/[id]/sharepic` is a small public page (preview, short instructions, download), not the image itself. Artists open it from mail on a phone and need an actual PNG file for Instagram; a naked image URL and client-side screenshots both fail that. Chosen options go into the download request, not into a stored file.
