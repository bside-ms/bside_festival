enum BlockType {
    checklist = 'checklist',
    code = 'code',
    delimiter = 'delimiter',
    header = 'header',
    image = 'image',
    list = 'list',
    paragraph = 'paragraph',
    quote = 'quote',
    raw = 'raw',
    table = 'table',
    warning = 'warning',
}

export default BlockType;
